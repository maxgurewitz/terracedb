# Sandbox Notes Project

This tiny project is the companion tree for the `terracedb-example-sandbox-notes` host app.

The host application hoists this directory into a sandbox session, injects a small note-store capability, runs `src/review.js`, checks and emits `src/render.ts`, and then exports the generated files back out to disk or a git worktree.

